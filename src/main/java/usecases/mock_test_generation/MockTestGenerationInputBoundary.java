package usecases.mock_test_generation;

/**
 * The mock test writing use case.
 */

public interface MockTestGenerationInputBoundary {
    /**
     * Executes the mock test generation use case so users can write it.
     */
    void execute(MockTestGenerationInputData mockTestGenerationInputData);
}
